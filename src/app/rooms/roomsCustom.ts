export interface RoomNumbers    {
    bookedRooms: number;
    availableRooms: number;
}

export interface RoomList   {
    roomType: string;
    price: number;
    checkinDate: Date;
    chances: number;
}